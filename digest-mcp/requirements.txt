# MCP SDK 2.x: клиент к sl-tracker-mcp + собственный сервер (Streamable HTTP/stdio).
# Тянет httpx2, starlette, uvicorn — отдельно их пинить не нужно.
mcp==2.2.0
# Рассылка в Telegram Bot API. Отдельно от httpx2 из mcp: у них разные мажоры.
httpx==0.28.1
# zoneinfo в slim-образе опирается на системный tzdata. RFC пишет, что его хватает,
# но пакет весит ~450 КБ и снимает целый класс отказов (в т.ч. на Windows при
# локальном прогоне тестов), поэтому ставим явно.
tzdata==2026.4
